{
  "name": "Hermes-CodeSpace",
  "customizations": {
    "vscode": {
      "extensions": ["saoudrizwan.claude-dev","joaompfp.hermes-ai-agent"]
    }
  },
  "postCreateCommand": "sudo npm install modelrelay -g --prefix /usr/local/lib/modelrelay; sudo  ln -s  /usr/local/lib/modelrelay/bin/modelrelay /usr/local/bin/modelrelay; ./.devcontainer/init-hermes.sh > /tmp/init-hermes.log",
  "postStartCommand": "modelrelay > /tmp/modelrelay.log"
}